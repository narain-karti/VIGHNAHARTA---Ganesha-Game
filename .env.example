# Copy to .env and fill in (never commit .env)
PORT=8080
# ElevenLabs voice API key — required only for /api/tts and generate_voices.py.
# Get one at https://elevenlabs.io — leave unset to run the game without TTS.
ELEVENLABS_API_KEY=
